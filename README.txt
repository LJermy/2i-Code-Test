Assumptions made:

Numbers iterated through will output to console
Values will be integers
The only input from users will be numbers
Iterating stops after passing the upper limit (x) rather than rounding down to the upper limit once it passes.